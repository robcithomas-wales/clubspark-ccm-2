{
  "name": "@clubspark/auth",
  "version": "1.0.0",
  "private": true,
  "description": "Shared authentication for ClubSpark services: tenant context, JWT verification, internal service-to-service auth.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "jose": "^6.0.0"
  },
  "devDependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "fastify": "^4.28.1",
    "jose": "^6.2.1",
    "reflect-metadata": "^0.2.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.0"
  }
}
